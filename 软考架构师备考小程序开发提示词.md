# 软考高级·系统架构设计师备考小程序开发任务

## 项目概述

开发一款微信小程序资格证书考试复习系统，备考方向为软考高级·系统架构设计师。

### 核心特性

- **AI题库生成**：用户上传文档（PDF/Word）或粘贴文本，AI自动分析并生成符合考试大纲的题目
- **三种考试模式**：综合知识（选择题）、案例分析、论文写作
- **极简线条UI**：工程蓝图风格，去除所有装饰性元素，仅用线条和文字

---

## 第一部分：基础功能需求

### 一、技术架构要求

#### 前端

- 微信小程序原生开发
- 使用WXML/WXSS/JS/TS
- 采用Vant Weapp或自定义组件库

#### 后端

- Node.js + Express/Koa 或 Python + Flask/FastAPI
- 数据库：MySQL或MongoDB
- 文件存储：本地或云存储（OSS/COS）

#### AI服务

- 调用大语言模型API（OpenAI/Claude/国产大模型）
- 支持PDF/Docx解析（pdf-parse、mammoth）

### 二、功能模块详细说明

#### 1. 首页/概览模块

- 展示考试科目信息（综合知识、案例分析、论文写作）
- 显示学习进度、错题统计
- 快捷入口：继续刷题、AI生成题库

#### 2. 刷题/题库模块

##### 综合知识（选择题）

- 75道单项选择题，每题1分
- 4个选项（A/B/C/D）
- 支持按章节/考点筛选练习
- 倒计时功能（150分钟）
- 答题卡视图（已答/未答状态）
- 提交后显示答案解析

##### 案例分析

- 5道大题：1必答 + 4选2 = 共做3题
- 每题约25分
- 题型包括：
  - 架构选型与设计（场景化方案、理由说明）
  - 性能/可用性/安全优化方案
  - 架构缺陷识别与改进
  - 分布式事务、缓存、分库分表
  - 架构评估（如ATAM）与技术决策
- 界面：案例背景在上，问题列表在下
- 选题限制逻辑：必须选3题，否则提示

##### 论文写作

- 4选1命题论文
- 要求2500字左右
- 功能：
  - 摘要框（300-400字）
  - 正文框（2000-2500字）
  - 实时字数统计
  - AI批改评分（结构分析+改进建议）

#### 3. AI题库生成模块

- 上传文档（PDF/Word，最大10MB）
- 粘贴文本
- 设置生成偏好：
  - 选择题数量（5/10/15/20题）
  - 是否包含案例分析
  - 是否包含论文题目
- 考点标签选择：
  - 计算机基础、操作系统、数据库、网络、安全
  - 软件工程、架构设计（4+1视图、架构风格、微服务、DDD、云原生）
  - 分布式、性能与可靠性、专业英语

#### 4. 我的模块

- 我的题库列表
- 错题本（自动收集，按考点筛选）
- 收藏夹
- 设置（主题、字体大小）

### 三、UI/UX设计规格

#### 设计风格

- **工程蓝图风格**：极致简约，呼应"架构师"主题
- 无阴影、无渐变、无圆角
- 仅使用线条和文字

#### 色板

- 背景：#FFFFFF（纯白）
- 主色/线条：#000000（纯黑）
- 强调色：#2563EB（工程蓝，用于高亮正确答案）
- 错误色：#DC2626（红色，用于错误答案）

#### 组件规范

- 按钮：矩形、直角、1px黑边框
- 输入框：底部一条黑线或矩形黑框
- 卡片：1px黑色边框，无阴影
- 进度条：空心黑条，内部填充黑色块
- 字体：等宽字体(Monospace)或系统默认黑体

#### 布局

- 底部TabBar：首页、刷题、AI生成、我的
- 头部：大字号标题，下方2px实线分割
- 操作区：底部悬浮栏

### 四、数据结构设计

#### 题目表 (questions)

```json
{
  "id": "q_xxxxx",
  "type": "choice|case|essay",
  "category": "架构设计|数据库|网络|...",
  "difficulty": 1-5,
  "content": "题目内容",
  "options": ["A. ...", "B. ...", "C. ...", "D. ..."],
  "correct_answer": "C",
  "explanation": "解析内容",
  "case_background": "案例背景描述",
  "case_questions": ["问题1", "问题2"],
  "source_doc_id": "doc_xxx",
  "created_at": "2024-01-01"
}
```

#### 试卷表 (papers)

```json
{
  "id": "paper_xxx",
  "type": "综合知识|案例分析|论文写作",
  "title": "2024年架构设计师模拟卷",
  "questions": ["q1", "q2", ...],
  "duration": 150,
  "total_score": 75
}
```

#### 用户做题记录 (user_answers)

```json
{
  "id": "answer_xxx",
  "user_id": "user_xxx",
  "question_id": "q_xxx",
  "answer": "C",
  "is_correct": true,
  "answered_at": "2024-01-01 10:00:00"
}
```

#### 文档表 (documents)

```json
{
  "id": "doc_xxx",
  "user_id": "user_xxx",
  "filename": "微服务架构笔记.pdf",
  "file_path": "/uploads/xxx.pdf",
  "content_text": "提取的文本内容",
  "generated_questions": 20,
  "uploaded_at": "2024-01-01"
}
```

### 五、AI题库生成Prompt策略

#### 选择题生成Prompt

```
基于以下文档内容，生成{数量}道关于{考点}的单项选择题。

要求：
1. 每题4个选项，仅1个正确答案
2. 答案需有详细解析
3. 题目难度适中，符合软考高级标准
4. 涵盖以下考点：{考点列表}

文档内容：
{文档文本}
```

#### 案例分析题生成Prompt

```
基于以下架构文档，生成1道案例分析题。

要求：
1. 包含完整的案例背景描述（约500字）
2. 提出2-3个具体问题
3. 问题需涉及架构选型、性能优化、可靠性设计等
4. 提供答题要点和参考答案

文档内容：
{文档文本}
```

#### 论文写作题生成Prompt

```
基于软考系统架构设计师考试要求，生成1道论文写作题目。

要求：
1. 题目应涵盖以下主题之一：微服务架构、云原生、高并发、架构演进
2. 提供写作要求和评分标准
3. 给出优秀论文结构框架

考点：{选择的考点}
```

### 六、验收标准

1. 完整支持软考高级三门考试形式（选择、案例、论文）
2. AI文档解析+题库生成总耗时不超过60秒
3. UI严格遵循"线条+文字"极简风格
4. 标准考点答案准确率≥95%
5. 案例分析选题限制逻辑正确
6. 论文字数统计准确，AI批改功能可用

---

## 第二部分：技术规格说明（已确定技术栈）

### 技术栈（已确定）

- **前端**：微信小程序原生开发（WXML/WXSS/JS）
- **后端**：Node.js + Express（或Koa）
- **数据库**：PostgreSQL
- **文件存储**：本地文件系统
- **AI服务**：调用大语言模型API

### 一、数据库设计（PostgreSQL）

#### 表1：用户表 (users)

```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  openid VARCHAR(128) UNIQUE,
  nickname VARCHAR(100),
  avatar_url VARCHAR(500),
  quota_remaining INT DEFAULT 50,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 表2：题库表 (question_banks)

```sql
CREATE TABLE question_banks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  title VARCHAR(200),
  source_type VARCHAR(20),
  source_content TEXT,
  source_filename VARCHAR(255),
  question_count INT DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 表3：题目表 (questions)

```sql
CREATE TABLE questions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  bank_id UUID REFERENCES question_banks(id) ON DELETE CASCADE,
  type VARCHAR(20) NOT NULL,
  category VARCHAR(50),
  difficulty INT DEFAULT 3,
  content TEXT NOT NULL,
  options JSONB,
  correct_answer TEXT,
  explanation TEXT,
  case_background TEXT,
  case_questions JSONB,
  essay_requirements TEXT,
  essay_template TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 表4：做题记录表 (practice_records)

```sql
CREATE TABLE practice_records (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  bank_id UUID REFERENCES question_banks(id),
  question_id UUID REFERENCES questions(id),
  answer TEXT,
  is_correct BOOLEAN,
  score INT,
  time_spent INT,
  answered_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 表5：错题本 (wrong_answers)

```sql
CREATE TABLE wrong_answers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  question_id UUID REFERENCES questions(id),
  wrong_count INT DEFAULT 1,
  last_wrong_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 二、API接口设计（RESTful）

#### 认证模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/login | 微信登录，获取token |

#### 题库管理模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/banks | 获取用户题库列表 |
| GET | /api/banks/:id | 获取题库详情（含题目） |
| POST | /api/banks | 创建空题库 |
| DELETE | /api/banks/:id | 删除题库 |

#### AI生成模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/generate/text | 文本生成题目 |
| POST | /api/generate/file | 文件上传生成题目 |
| GET | /api/generate/status/:taskId | 查询生成状态 |

#### 刷题模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/practice/choice | 获取选择题（可指定数量/分类） |
| GET | /api/practice/case | 获取案例分析题 |
| GET | /api/practice/essay | 获取论文题目 |
| POST | /api/practice/submit | 提交答案 |
| GET | /api/practice/wrong | 获取错题本 |

#### 用户模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/user/profile | 获取用户信息 |
| PUT | /api/user/profile | 更新用户信息 |
| GET | /api/user/stats | 获取学习统计 |

### 三、后端项目结构

```
project-root/
├── src/
│   ├── config/
│   │   ├── database.js
│   │   └── upload.js
│   ├── controllers/
│   │   ├── authController.js
│   │   ├── bankController.js
│   │   ├── generateController.js
│   │   ├── practiceController.js
│   │   └── userController.js
│   ├── middlewares/
│   │   ├── auth.js
│   │   ├── error.js
│   │   └── upload.js
│   ├── models/
│   │   ├── user.js
│   │   ├── questionBank.js
│   │   ├── question.js
│   │   └── practiceRecord.js
│   ├── routes/
│   │   ├── auth.js
│   │   ├── banks.js
│   │   ├── generate.js
│   │   ├── practice.js
│   │   └── user.js
│   ├── services/
│   │   ├── aiService.js
│   │   ├── fileService.js
│   │   └── questionService.js
│   ├── utils/
│   │   ├── promptBuilder.js
│   │   └── pdfParser.js
│   ├── app.js
│   └── server.js
├── uploads/
│   ├── temp/
│   └── docs/
├── .env
└── package.json
```

### 四、环境变量配置 (.env)

```env
PORT=3000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_NAME=arch_exam
DB_USER=postgres
DB_PASSWORD=your_password

JWT_SECRET=your_jwt_secret_key

UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

AI_PROVIDER=openai
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4o-mini
```

### 五、核心业务逻辑

#### 1. AI题库生成流程

```
用户上传文件/粘贴文本
        ↓
文件解析（pdf-parse/mammoth）
        ↓
文本提取 + 预处理
        ↓
构建AI提示词（Prompt）
        ↓
调用LLM生成题目（JSON格式）
        ↓
解析JSON + 数据验证
        ↓
存储到PostgreSQL
        ↓
返回题库ID给前端
```

#### 2. AI生成Prompt模板

**选择题生成**：

```
你是一位软考高级系统架构设计师考试的辅导老师。
请基于以下文档内容，生成{数量}道关于{考点}的单项选择题。

要求：
1. 每题4个选项（A/B/C/D），仅1个正确答案
2. 答案需有详细解析
3. 题目需符合软考高级难度标准
4. 必须输出严格JSON格式，不能有其他内容

输出格式：
[{"type":"choice","content":"题目","options":[{"label":"A","text":"选项A"},{"label":"B","text":"选项B"},{"label":"C","text":"选项C"},{"label":"D","text":"选项D"}],"correct_answer":"A","explanation":"解析","category":"考点","difficulty":3}]

文档内容：
{文本内容}
```

**案例分析题生成**：

```
基于以下架构文档，生成1道案例分析题。

要求：
1. 包含完整的案例背景描述（300-500字）
2. 提出2-3个具体问题
3. 问题需涉及架构选型、性能优化、可靠性设计等
4. 提供答题要点

输出格式：
{"type":"case","case_background":"背景","case_questions":["问题1","问题2"],"answer_points":["要点1","要点2"],"category":"架构设计","difficulty":4}
```

#### 3. 文件处理（本地存储）

- 使用 `multer` 处理文件上传
- 文件保存路径：`uploads/{year}/{month}/{timestamp}_{filename}`
- 支持格式：PDF、DOCX、TXT
- 文件大小限制：10MB

### 六、前端页面设计（微信小程序）

#### 页面结构

```
pages/
├── index/
├── bank/
├── bank/detail/
├── generate/
├── practice/choice/
├── practice/case/
├── practice/essay/
├── wrong/
└── profile/
```

### 七、关键考点（软考高级·系统架构设计师）

#### 综合知识（选择题）

- 计算机基础、操作系统、数据库原理
- 计算机网络、安全基础知识
- 软件工程、需求工程
- 架构设计：4+1视图、架构风格（分层、MVC、微服务）
- 领域驱动设计（DDD）
- 分布式系统、云原生架构
- 性能设计、可靠性设计
- 专业英语（5题左右）

#### 案例分析

- 架构选型与设计（场景化方案）
- 性能/可用性/安全优化方案
- 架构缺陷识别与改进
- 分布式事务、缓存、分库分表
- 架构评估（ATAM）
- 质量属性效用树

#### 论文写作

- 微服务/云原生架构
- 高并发/高可用系统
- 数据架构、企业集成
- 架构演进与优化
- 新技术应用（AI、大数据、区块链）

---

## 考试内容参考

### 一、综合知识（上午，150分钟）

- **题型**：75道单项选择题，每题1分，满分75。

- **核心考点**：
  - 计算机基础、操作系统、数据库、网络、安全
  - 软件工程、架构设计（4+1视图、架构风格、微服务、DDD等）
  - 分布式、云原生、性能与可靠性、专业英语（约5题）

### 二、案例分析（下午第一场，90分钟）

- **题型**：5道大题 → 1必答 + 4选2，共做3题，每题约25分，满分75。

- **常见题型**：
  - 架构选型与设计（场景化方案、理由说明）
  - 性能/可用性/安全优化方案
  - 架构缺陷识别与改进
  - 分布式事务、缓存、分库分表等实战问题
  - 架构评估（如ATAM）与技术决策

### 三、论文写作（下午第二场，120分钟）

- **题型**：4选1命题论文，要求2500字左右，满分75。

- **常见主题**：
  - 微服务/云原生架构设计与实践
  - 高并发/高可用系统架构
  - 数据架构、企业集成架构
  - 架构演进、性能优化、安全架构
  - 新技术（AI、大数据、区块链）在架构中的应用
